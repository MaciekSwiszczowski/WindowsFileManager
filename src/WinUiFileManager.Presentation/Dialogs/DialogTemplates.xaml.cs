namespace WinUiFileManager.Presentation.Dialogs;

/// <summary>
/// Code-behind for the <c>DialogTemplates</c> XAML resource dictionary, which holds the
/// <see cref="Microsoft.UI.Xaml.DataTemplate"/>s that <see cref="WinUiFileManager.Presentation.Services.DialogService"/>
/// resolves by key when building dialog content. Contains no logic beyond loading the XAML.
/// </summary>
public sealed partial class DialogTemplates
{
    public DialogTemplates()
    {
        InitializeComponent();
    }
}
