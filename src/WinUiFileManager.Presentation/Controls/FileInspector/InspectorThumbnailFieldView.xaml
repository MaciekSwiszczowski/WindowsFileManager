<UserControl
    x:Class="WinUiFileManager.Presentation.Controls.FileInspector.InspectorThumbnailFieldView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>

    <Border Background="Transparent"
            PointerEntered="OnRowPointerEntered"
            PointerExited="OnRowPointerExited"
            ToolTipService.ToolTip="{x:Bind Field.Tooltip, Mode=OneWay}"
            Visibility="{x:Bind Field.IsVisible, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
        <StackPanel Orientation="Horizontal">
            <TextBlock Width="88"
                       Padding="6,3"
                       Text="{x:Bind Field.Key, Mode=OneWay}"
                       FontSize="12"
                       TextWrapping="Wrap" />
            <ProgressRing Width="14"
                          Height="14"
                          Margin="6,3"
                          IsActive="True"
                          Visibility="{x:Bind Field.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
            <Image Width="30"
                   Height="30"
                   Margin="6,3"
                   Source="{x:Bind Field.ThumbnailSource, Mode=OneWay}"
                   Stretch="UniformToFill"
                   Visibility="{x:Bind Field.ShowsThumbnail, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
            <TextBlock Width="118"
                       Padding="6,3"
                       Text="{x:Bind Field.DisplayValue, Mode=OneWay}"
                       FontSize="12"
                       TextTrimming="CharacterEllipsis"
                       TextWrapping="Wrap"
                       Visibility="{x:Bind Field.IsUnavailable, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
        </StackPanel>
    </Border>
</UserControl>
