<UserControl
    x:Class="WinUiFileManager.Presentation.Controls.FileInspector.InspectorTextFieldView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>

    <Border Background="Transparent"
            HorizontalAlignment="Stretch"
            PointerEntered="OnRowPointerEntered"
            PointerExited="OnRowPointerExited"
            ToolTipService.ToolTip="{x:Bind BasicField.Tooltip, Mode=OneWay}"
            Visibility="{x:Bind BasicField.IsVisible, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
        <Grid ColumnSpacing="0"
              HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="88" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Padding="6,3"
                       Text="{x:Bind BasicField.Key, Mode=OneWay}"
                       FontSize="12"
                       TextWrapping="Wrap" />
            <ProgressRing Grid.Column="1"
                          Width="14"
                          Height="14"
                          Margin="6,3"
                          IsActive="True"
                          Visibility="{x:Bind BasicField.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
            <TextBlock Grid.Column="2"
                       Padding="6,3"
                       IsTextSelectionEnabled="True"
                       Text="{x:Bind BasicField.DisplayValue, Mode=OneWay}"
                       FontSize="12"
                       TextTrimming="CharacterEllipsis"
                       TextWrapping="Wrap" />
        </Grid>
    </Border>
</UserControl>
