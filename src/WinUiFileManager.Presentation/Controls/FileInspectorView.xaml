<UserControl
    x:Class="WinUiFileManager.Presentation.Controls.FileInspectorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>

    <Border Padding="8"
            BorderBrush="{ThemeResource SystemControlBackgroundBaseLowBrush}"
            BorderThickness="1"
            Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid>
            <TextBlock HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                       Text="{Binding SelectionStatusText}"
                       TextAlignment="Center"
                       Visibility="{Binding IsNoSelectionMode, Converter={StaticResource BoolToVisibilityConverter}}" />

            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Spacing="4"
                        Visibility="{Binding IsMultiSelectionMode, Converter={StaticResource BoolToVisibilityConverter}}">
                <TextBlock HorizontalAlignment="Center"
                           Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                           Text="{Binding SelectionStatusText}"
                           TextAlignment="Center" />
                <TextBlock HorizontalAlignment="Center"
                           FontSize="12"
                           Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                           Text="Inspector details are shown for a single item."
                           TextAlignment="Center" />
            </StackPanel>

            <Grid RowSpacing="6"
                  Visibility="{Binding IsSingleSelectionMode, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" ColumnSpacing="6">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Text="Inspector" />
                    <Button Grid.Column="1"
                            Command="{Binding Commands.RefreshCommand}"
                            Content="Refresh" />
                    <Button Grid.Column="2"
                            Command="{Binding Commands.ShowPropertiesCommand}"
                            Content="Properties" />
                    <Button Grid.Column="3"
                            Command="{Binding Commands.CopyAllCommand}"
                            Content="Copy All" />
                </Grid>

                <TextBox Grid.Row="1"
                         PlaceholderText="Search property or value"
                         Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <ScrollViewer Grid.Row="2"
                              HorizontalScrollBarVisibility="Auto"
                              VerticalScrollBarVisibility="Auto">
                    <Grid MinWidth="520">
                        <ItemsRepeater ItemsSource="{Binding Categories}">
                            <ItemsRepeater.Layout>
                                <StackLayout Spacing="4" />
                            </ItemsRepeater.Layout>
                            <ItemsRepeater.ItemTemplate>
                                <DataTemplate>
                                    <Expander Header="{Binding Name}"
                                              Visibility="{Binding HasVisibleFields, Converter={StaticResource BoolToVisibilityConverter}}"
                                              IsExpanded="{Binding IsExpanded, Mode=TwoWay}"
                                              HorizontalAlignment="Stretch"
                                              HorizontalContentAlignment="Stretch">
                                        <ItemsRepeater ItemsSource="{Binding Fields}">
                                            <ItemsRepeater.Layout>
                                                <StackLayout />
                                            </ItemsRepeater.Layout>
                                            <ItemsRepeater.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal"
                                                                Visibility="{Binding IsVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                                                        <TextBlock Width="128"
                                                                   Padding="8,4"
                                                                   Text="{Binding Key}"
                                                                   FontSize="12" />
                                                        <ProgressRing Width="14"
                                                                      Height="14"
                                                                      Margin="8,4"
                                                                      IsActive="True"
                                                                      Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}" />
                                                        <TextBlock Width="330"
                                                                   Padding="8,4"
                                                                   Text="{Binding DisplayValue}"
                                                                   FontSize="12"
                                                                   TextTrimming="CharacterEllipsis"
                                                                   TextWrapping="Wrap"
                                                                   Visibility="{Binding ShowsValue, Converter={StaticResource BoolToVisibilityConverter}}" />
                                                        <CheckBox VerticalAlignment="Center"
                                                                  IsChecked="{Binding IsToggleOn}"
                                                                  IsEnabled="{Binding CanInteract}"
                                                                  Command="{Binding ToggleCommand}"
                                                                  Visibility="{Binding ShowsToggle, Converter={StaticResource BoolToVisibilityConverter}}" />
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsRepeater.ItemTemplate>
                                        </ItemsRepeater>
                                    </Expander>
                                </DataTemplate>
                            </ItemsRepeater.ItemTemplate>
                        </ItemsRepeater>
                    </Grid>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Border>
</UserControl>
