namespace WinUiFileManager.Presentation.Controls.FileInspector;

/// <summary>
/// Inspector placeholder shown when nothing is selected (purely static XAML content prompting the user
/// to select an item). Has no view model.
/// </summary>
public sealed partial class InspectorNoSelectionView
{
    public InspectorNoSelectionView()
    {
        InitializeComponent();
    }
}
