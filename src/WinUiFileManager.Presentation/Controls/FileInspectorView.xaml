<UserControl
    x:Class="WinUiFileManager.Presentation.Controls.FileInspectorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:fileInspector="using:WinUiFileManager.Presentation.Controls.FileInspector"
    xmlns:viewModels="using:WinUiFileManager.Presentation.ViewModels">

    <Border Padding="8"
            BorderBrush="{ThemeResource SystemControlBackgroundBaseLowBrush}"
            BorderThickness="1"
            Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <controls:SwitchPresenter TargetType="viewModels:FileInspectorSelectionMode"
                                  Value="{x:Bind ViewModel.SelectionMode, Mode=OneWay}"
                                  HorizontalAlignment="Stretch"
                                  VerticalAlignment="Stretch">
            <controls:Case Value="NoSelection">
                <fileInspector:InspectorNoSelectionView />
            </controls:Case>
            <controls:Case Value="SingleSelection">
                <fileInspector:InspectorSingleSelectionView ViewModel="{x:Bind ViewModel, Mode=OneWay}" />
            </controls:Case>
            <controls:Case Value="MultiSelection">
                <fileInspector:InspectorMultiSelectionView ViewModel="{x:Bind ViewModel, Mode=OneWay}" />
            </controls:Case>
        </controls:SwitchPresenter>
    </Border>
</UserControl>
