<UserControl
    x:Class="WinUiFileManager.Presentation.MessageLogging.MessageLogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox
                x:Name="IdFilterBox"
                Header="Message id"
                PlaceholderText="Left, Right"
                TextChanged="IdFilterBox_TextChanged" />

            <ToggleButton
                x:Name="PauseButton"
                Grid.Column="1"
                MinWidth="84"
                VerticalAlignment="Bottom"
                Click="PauseButton_Click" />

            <Button
                Grid.Column="2"
                MinWidth="72"
                VerticalAlignment="Bottom"
                Click="ClearButton_Click">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <SymbolIcon Symbol="Clear" />
                    <TextBlock Text="Clear" />
                </StackPanel>
            </Button>
        </Grid>

        <ListView
            Grid.Row="1"
            ItemsSource="{x:Bind Store.Entries, Mode=OneWay}"
            SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock
                        Text="{Binding}"
                        FontFamily="Consolas"
                        FontSize="12"
                        TextWrapping="NoWrap" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
