<UserControl
    x:Class="WinUiFileManager.Presentation.Controls.FileInspectorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <Style x:Key="InspectorCellStyle" TargetType="TextBlock">
            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="IsTextSelectionEnabled" Value="True" />
        </Style>
    </UserControl.Resources>

    <Border Padding="12"
            BorderBrush="{ThemeResource SystemControlBackgroundBaseLowBrush}"
            BorderThickness="1"
            Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid x:Name="InspectorContentGrid"
              RowSpacing="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" ColumnSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="Inspector"
                           FontSize="18"
                           FontWeight="SemiBold" />

                <Button Grid.Column="1"
                        Content="Refresh"
                        Click="OnRefreshClick"
                        IsEnabled="{Binding HasItem}" />

                <Button Grid.Column="2"
                        Content="Properties"
                        Click="OnPropertiesClick"
                        IsEnabled="{Binding HasItem}" />

                <Button Grid.Column="3"
                        Content="Copy All"
                        Click="OnCopyAllClick"
                        IsEnabled="{Binding HasItem}" />
            </Grid>

            <StackPanel Grid.Row="1" Spacing="4">
                <TextBlock Text="{Binding StatusMessage}"
                           Visibility="{Binding StatusMessageVisibility}"
                           Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
            </StackPanel>

            <TextBox Grid.Row="2"
                     x:Name="SearchBox"
                     PlaceholderText="Search property or value"
                     Visibility="{Binding SearchVisibility}"
                     TextChanged="OnSearchTextChanged" />

            <ScrollViewer Grid.Row="3"
                          Visibility="{Binding DetailsVisibility}"
                          HorizontalScrollBarVisibility="Disabled"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalAlignment="Stretch">
                <Grid Width="{Binding InspectorContentWidth}"
                      HorizontalAlignment="Left">
                    <ItemsRepeater ItemsSource="{Binding Categories}">
                        <ItemsRepeater.Layout>
                            <StackLayout Spacing="8" />
                        </ItemsRepeater.Layout>
                        <ItemsRepeater.ItemTemplate>
                            <DataTemplate>
                                <Expander Header="{Binding Name}"
                                          Visibility="{Binding Visibility}"
                                          IsExpanded="{Binding IsExpanded, Mode=TwoWay}"
                                          Width="{Binding ContentWidth}"
                                          HorizontalAlignment="Stretch"
                                          HorizontalContentAlignment="Stretch">
                                    <Border BorderBrush="{ThemeResource SystemControlBackgroundBaseLowBrush}"
                                            BorderThickness="1"
                                            CornerRadius="4"
                                            HorizontalAlignment="Stretch">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>

                                            <Grid Grid.Row="0"
                                                  Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="180" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>

                                                <Border Grid.Column="0"
                                                        Padding="16,10"
                                                        BorderBrush="{ThemeResource SystemControlBackgroundBaseLowBrush}"
                                                        BorderThickness="0,0,1,1">
                                                    <TextBlock Text="Property"
                                                               FontWeight="SemiBold" />
                                                </Border>

                                                <Border Grid.Column="1"
                                                        Padding="16,10"
                                                        BorderBrush="{ThemeResource SystemControlBackgroundBaseLowBrush}"
                                                        BorderThickness="0,0,0,1">
                                                    <TextBlock Text="Value"
                                                               FontWeight="SemiBold" />
                                                </Border>
                                            </Grid>

                                            <ItemsRepeater Grid.Row="1"
                                                           ItemsSource="{Binding Fields}">
                                                <ItemsRepeater.Layout>
                                                    <StackLayout />
                                                </ItemsRepeater.Layout>
                                                <ItemsRepeater.ItemTemplate>
                                                    <DataTemplate>
                                                        <Grid Visibility="{Binding RowVisibility}">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="180" />
                                                                <ColumnDefinition Width="*" />
                                                            </Grid.ColumnDefinitions>

                                                            <Border Grid.Column="0"
                                                                    Padding="16,10"
                                                                    BorderBrush="{ThemeResource SystemControlBackgroundBaseLowBrush}"
                                                                    BorderThickness="0,0,1,1"
                                                                    Background="Transparent"
                                                                    ToolTipService.ToolTip="{Binding Tooltip}">
                                                                <TextBlock Text="{Binding Key}"
                                                                           Style="{StaticResource InspectorCellStyle}" />
                                                            </Border>

                                                            <Border Grid.Column="1"
                                                                    Padding="16,10"
                                                                    BorderBrush="{ThemeResource SystemControlBackgroundBaseLowBrush}"
                                                                    BorderThickness="0,0,0,1"
                                                                    Background="Transparent">
                                                                <Grid>
                                                                    <StackPanel Orientation="Horizontal"
                                                                                Spacing="8"
                                                                                VerticalAlignment="Center"
                                                                                Visibility="{Binding LoadingVisibility}">
                                                                        <ProgressRing Width="14"
                                                                                      Height="14"
                                                                                      IsActive="True" />
                                                                        <TextBlock Text="{Binding DisplayValue}"
                                                                                   Style="{StaticResource InspectorCellStyle}" />
                                                                    </StackPanel>
                                                                    <TextBlock Text="{Binding DisplayValue}"
                                                                               Style="{StaticResource InspectorCellStyle}"
                                                                               Visibility="{Binding ValueVisibility}" />
                                                                    <Border Width="30"
                                                                            Height="30"
                                                                            Padding="0"
                                                                            Margin="0"
                                                                            HorizontalAlignment="Left"
                                                                            VerticalAlignment="Center"
                                                                            Visibility="{Binding ThumbnailVisibility}">
                                                                        <Image Source="{Binding ThumbnailSource}"
                                                                               Width="30"
                                                                               Height="30"
                                                                               HorizontalAlignment="Stretch"
                                                                               VerticalAlignment="Stretch"
                                                                               Stretch="UniformToFill" />
                                                                    </Border>
                                                                </Grid>
                                                            </Border>
                                                        </Grid>
                                                    </DataTemplate>
                                                </ItemsRepeater.ItemTemplate>
                                            </ItemsRepeater>
                                        </Grid>
                                    </Border>
                                </Expander>
                            </DataTemplate>
                        </ItemsRepeater.ItemTemplate>
                    </ItemsRepeater>
                </Grid>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
