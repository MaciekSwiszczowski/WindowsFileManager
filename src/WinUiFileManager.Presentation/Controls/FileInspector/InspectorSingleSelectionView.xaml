<UserControl
    x:Class="WinUiFileManager.Presentation.Controls.FileInspector.InspectorSingleSelectionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:fileInspector="using:WinUiFileManager.Presentation.Controls.FileInspector"
    xmlns:inspector="using:WinUiFileManager.Presentation.ViewModels.Inspector">

    <Grid RowSpacing="6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" ColumnSpacing="6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Text="Inspector" />
            <Button Grid.Column="1"
                    Command="{x:Bind ViewModel.Commands.RefreshCommand}"
                    Content="Refresh" />
            <Button Grid.Column="2"
                    Command="{x:Bind ViewModel.Commands.ShowPropertiesCommand}"
                    Content="Properties" />
            <Button Grid.Column="3"
                    Command="{x:Bind ViewModel.Commands.CopyAllCommand}"
                    Content="Copy All" />
        </Grid>

        <TextBox Grid.Row="1"
                 PlaceholderText="Search property or value"
                 Text="{x:Bind ViewModel.SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        <ScrollViewer Grid.Row="2"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto">
            <Grid MinWidth="250">
                <ItemsRepeater ItemsSource="{x:Bind ViewModel.Categories, Mode=OneWay}">
                    <ItemsRepeater.Layout>
                        <StackLayout Spacing="4" />
                    </ItemsRepeater.Layout>
                    <ItemsRepeater.ItemTemplate>
                        <DataTemplate x:DataType="inspector:InspectorCategoryViewModel">
                            <fileInspector:InspectorCategoryView Category="{x:Bind}" />
                        </DataTemplate>
                    </ItemsRepeater.ItemTemplate>
                </ItemsRepeater>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
